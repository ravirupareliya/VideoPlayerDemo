import { StyleSheet } from 'react-native'
import { ApplicationStyles, Colors } from '../../Themes/'

export default StyleSheet.create({
    ...ApplicationStyles.screen,

    loaderView: { alignSelf: 'center', flex: 1, justifyContent: 'center' }

})